/**
 * @fileoverview Maahes shared utilities.
 *
 * Generic helpers shared across every Maahes module (config merging,
 * common types). Module-specific code lives next to its module.
 *
 * @module shared
 */

export * from "./deepMerge";